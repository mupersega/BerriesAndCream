export enum AgentRole {
  Idle,
  Scout,      // Explores and maps the area
  Forager,    // Gathers food resources
  WaterBearer, // Collects and brings water to base
  Builder,    // Constructs and maintains structures
  Guard       // Protects the colony from threats (future feature)
} 